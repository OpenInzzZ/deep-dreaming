/**
 * Host loader entry for the plugin-manager browser implementation exported
 * from `./client`. The node half has no behavior of its own: the Loader row
 * exists so the entry mounts (and the client-modules scan discovers the
 * `dsh.client` declaration below).
 */
export function apply() {}
